/**
 * Tabs Constants
 */

/** The tablist keyboard contract (WAI-ARIA "tabs" pattern, automatic activation). */
export const TAB_KEY = {
  END: 'End',
  HOME: 'Home',
  NEXT: 'ArrowRight',
  PREVIOUS: 'ArrowLeft',
} as const;
