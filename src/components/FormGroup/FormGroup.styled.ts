/**
 * FormGroup Styled Components
 */

import styled from 'styled-components';

import { s } from '../../tokens/css-variables';

export const StyledFormGroup = styled.div`
  margin-bottom: ${s('sm')};
`;
