/**
 * Internal icon module — React Native resolution
 *
 * Native counterpart of `index.ts`: same icon names, rendered with
 * `react-native-svg` under the hood. `lucide-react-native` is an OPTIONAL peer
 * dependency — native consumers install it (and `react-native-svg`) themselves.
 */

export * from 'lucide-react-native';
